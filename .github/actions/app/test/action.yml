name: 'Test Application'
description: 'Run GO Tests and Image Test'
runs:
  using: "composite"
  steps:
    - uses: actions/setup-go@v4
      with:
        go-version: 1.22
    - shell: bash
      run: go test -v ./...
    - shell: bash
      run: ./scripts/app-test-end2end.sh
